function calcular(n1: number, n2: number): number {
  return n1 * n2;
}

function saudacao(nome: string): string {
  return `Olá, eu sou o ${nome}. :D`
}

console.log(calcular(5, 5));
console.log(saudacao("Anderson"));